apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: pipe-name-
spec:
  entrypoint: entry
  templates:
  - name: entry
    dag:
      tasks:
      - name: step-A
        templateRef:
          name: root-step
          template: suspend
      - name: step-B
        templateRef:
          name: whalesay
          template: whalesay-template
        dependencies: [step-A]
        arguments:
          parameters:
          - name: message
            value: "{{workflow.parameters.step-B-message}}"
      - name: step-C
        templateRef:
          name: whalesay
          template: whalesay-template
        dependencies: [step-A]
        arguments:
          parameters:
          - name: message
            value: "{{workflow.parameters.step-C-message}}"
      - name: step-D
        templateRef:
          name: whalesay
          template: whalesay-template
        dependencies: [step-A]
        arguments:
          parameters:
          - name: message
            value: "{{workflow.parameters.step-D-message}}"
      - name: step-E
        templateRef:
          name: whalesay
          template: whalesay-template
        dependencies: [step-A]
        arguments:
          parameters:
          - name: message
            value: "{{workflow.parameters.step-E-message}}"
      - name: step-F
        templateRef:
          name: whalesay
          template: whalesay-template
        dependencies: [step-A]
        arguments:
          parameters:
          - name: message
            value: "{{workflow.parameters.step-F-message}}"
      - name: step-G
        templateRef:
          name: maintenance-step
          template: maintenance-mode
        dependencies: [step-D]
