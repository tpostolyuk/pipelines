apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: step-forever-
spec:
  entrypoint: entry
  templates:
  - name: entry
    dag:
      tasks:
      - name: step-forever
        templateRef:
          name: forever
          template: forever
